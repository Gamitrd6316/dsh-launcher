# 更新日志 / Changelog

## v1.0.2 (2026-08-15)

**交互修复与一致性打磨**

### 🐛 修复

- **托盘主题切换全局生效**：托盘菜单切深浅主题后，**整个主窗口立即重建并弹出展示**新主题（此前只更新了菜单自身）；切换动作先执行再关闭弹层，避免相互干扰
- **托盘菜单点击崩溃加固**：所有菜单项"先执行动作再关闭"，任意操作异常都被捕获，**不再出现点击某项后程序直接退出**；主窗口隐藏时打开插件商城会先弹出主窗口（避免 owned window 异常）
- **更新页与插件页判定统一**：`PullPlugin`（插件页"检查更新"）与 `CheckUpdates`（更新页"可更新"）改用**完全一致的判定策略**——`fetch 当前分支 → 计算本地落后提交数`，只有真正落后才显示可更新/执行拉取；不再用陈旧的 `@{u}` 引用误判"已最新"，也不再哈希不同就误报"可更新"
- **git 操作自动走代理**：`RunGit` 自动附加 `-c http.proxy`，第三方插件仓库（需代理访问）的检查/拉取更可靠
- **更新页版本显示**：修复"当前 v1.0.1、最新 1.0.0"的版本号不同步问题，统一到 1.0.2

### ✨ 其他

- 设置页新增「ℹ️ 关于」对话框：中英双语介绍（软件定位、核心功能、8 语言）
- README 重写：突出"管理 dsh/插件、一键更新维护、小白友好"亮点，新增多语言文档与对应语言界面配图

## v1.0.1 (2026-08-15)

**插件依赖管理系统** — 根治"装个插件缺依赖 → 整个服务崩溃 → 前端全挂"的连锁故障。

### 🧩 插件依赖管理（本次核心）

- **安装后自动装依赖**：git/npm 安装插件完成后，自动解析 package.json → 缺失依赖自动从共享目录链接或 npm 补齐，从源头杜绝"装完就崩"
- **依赖状态徽章**：插件列表每行显示 `✓ 依赖完整` / `⚠️ 缺依赖: xxx`，一眼识别高危插件
- **一键修复依赖**：缺依赖的插件自动出现「🔧 修复依赖」按钮；「一键维护」同步升级为智能依赖检查（不再盲目 npm install）
- **坏插件自动隔离**：修复不了的插件自动挂 `.disabled` 后缀隔离，**保证服务正常启动**，不再拖垮整个插件树
- **玩家提示 + dsh 修复命令**：隔离后弹出提示，附上可执行的修复命令（`npm install -g <依赖>` / `cd <插件目录> && npm install`）
- **共享依赖目录**：`plugins/_shared-deps` junction 池统一复用 dsh 自带依赖（schemastery/cordis/react 等），一处依赖多处复用
- **智能排除内部目录**：`_shared-deps` 等下划线开头目录不再被误认为插件
- **支持嵌套 package.json**：插件依赖声明在子目录（`dsh-vision-toolkit/`、`injector/` 等）时也能正确识别

### 🐛 修复

- 修复插件缺依赖时服务端插件树崩溃、前端所有 client bundle 加载失败的连锁故障
- 修复全局 npm 目录探测（兼容 D:\npm-global / %USERPROFILE%\npm-global / %APPDATA%\npm）
- 修复 scoped 包（@scope/name）依赖在共享目录中的解析

## v1.0.0 (2026-08-15)

**正式版首发** — WPF 重构版（GPU 渲染 / 双主题 / 现代交互），此前 WinForms 试验版全部历史归档清理。

### 🎨 界面与质感

- **WPF 全面重写**：GPU 合成渲染、PerMonitorV2 任意 DPI 高清、无边框窗原生缩放/最大化/吸附（WindowChrome）
- **深色 / 浅色双主题**：设置页与托盘一键热切换，全控件主题感知，无白底白字/对比度崩塌
- **DeepSeek 品牌质感**：官方蓝渐变圆角 Logo（白鲸/蓝鲸随主题）、深海蓝调色、微渐变卡片、1px 高光边框、柔和投影
- **丝滑动效**：切页阻尼淡入上滑、导航项指示条展开、按钮悬停光晕、启动闪屏流光 + 白鲸光晕
- **现代系统托盘**：WPF 悬浮圆角卡片菜单（打开/启动/停止/重启/浏览器/商城/主题/退出），告别 WinForms 原生旧菜单
- **现代化弹窗**：全部提示/确认替换为深浅自适应的圆角微光对话框（✨/⚠️/❓ 图标标题）
- **日志页重构**：终端式深色代码区、源切换、实时搜索过滤、复制、清空；修复顶部空白断层
- 极简自适应滚动条、修复下拉框白底白字/蓝白割裂、修复浅色模式刺眼眩光

### ⚙️ 功能

- **一键安装（自定义路径）**：自动下载 Node.js（官方→镜像兜底），支持指定安装目录，自动持久化写入用户 PATH 与 launcher.json，指令全局可用
- **常驻服务状态监控器**：启动/停止/重启后 UI 与真实端口状态永远同步（修复"服务已重启但界面卡在未启动"）
- **插件智能更新**：仅当远程确有新提交才拉取（`@{u}` 落后数判定 + 无上游时对比远程默认分支），已最新显示 ✓ 绝不误报失败
- **更新页重构**：明确标注「🚀 启动器 (Launcher)」「🐋 Harness (dsh)」「📦 插件」，检查更新带转圈动画，按钮状态感知（已最新置灰），更新完成后立即刷新
- **插件商城多源聚合**：GitHub 多关键词检索 + npm 官方包源 + Awesome 列表，数百插件；已安装自动标记 ✓ 不可再点
- **环境检测增强**：nvm/scoop/choco/注册表 PATH 多策略自愈；环境未检测完成时点击主按钮先检测而非误判"未安装"
- **多语言**：中 / 英 / 日（国旗图标切换）
- **语义化版本比较**：启动器自更新改用 Version 语义比较，告别字符串硬编码误判

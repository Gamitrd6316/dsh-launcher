# 更新日志 / Changelog

## v1.4.5 (2026-08-14)

**稳定性加固（沙盒实测 + 全量代码审计后修复）**

- 🐛 修复 **git 在受限 PATH 下访问违例崩溃**（0xC0000005）：自动补全 git 依赖目录（mingw64\bin、usr\bin），解决"别人机器上插件检查/更新崩"的问题
- 🐛 修复 `where` 命令找不到时把错误提示当结果（导致"未检测到 dsh"误判）
- 🐛 修复 `npm root -g` 以程序名直启失败（改为 cmd /c）
- 🐛 修复命令执行管道死锁风险（stdout/stderr 并发排空 + 退出码校验）
- 🐛 修复每 3 小时自动检查的**跨线程 UI 访问崩溃**（全部回 UI 线程）
- 🐛 修复窗口缩放泄漏 GDI Region 句柄
- 🔍 **新增调试日志接口**：设置环境变量 `DSH_LAUNCHER_DEBUG=1` 后，关键流程/命令调用/耗时全部写入 `launcher-debug.log`
- 日志页「自动刷新」复选框真实生效；启动器「下载新版本」改为打开 GitHub Releases 页面；设置保存失败会明确报错；插件安装输入做注入防护
- 沙盒全链路实测：一键安装（npm 真实安装 103s）→ 一键启动（8199 端口监听）→ 停止服务 → 插件全部更新/启用禁用/可更新徽标 全部通过

## v1.4.4 (2026-08-14)

**零基础友好**

- 一键安装彻底去依赖：版本查询改用内置下载器（不再要求系统有 curl），自动识别 **32/64 位**系统选择对应 Node.js 包
- 数据目录默认改为标准约定 `~/.dsh`（与 dsh 官方一致），用户主目录不可用时才退回 exe 目录
- 设置页新增「桌面快捷方式」按钮：一键在桌面生成启动图标，小白无需再翻文件夹

## v1.4.3 (2026-08-14)

**窗口体验 & 自更新加固**

- 标题栏右上角新增**最大化/还原**按钮，双击标题栏也可切换；支持**四边拖拽缩放**窗口，全 UI 响应式
- 侧边栏底部与仓库加入开发者署名 **loudMore**
- 启动器自更新改为**多源链式获取**（配置源 → jsDelivr CDN），国内网络也能自动拿到最新版本

## v1.4.2 (2026-08-14)

**通用化 & 美化**

- 首页状态条改为**彩色圆角芯片**样式（dsh 版本 / 地址 / Node / npm / git / 插件数 / 更新状态），自动换行
- 插件支持**启用/禁用开关**（目录重命名加 `.disabled` 后缀，可逆无损），禁用插件显示橙色徽标
- 环境检测**通用化**：新增 nvm-windows / scoop / chocolatey 等常见 Node.js 安装路径与多版本扫描，Git 常见安装路径
- 缺 Git 时环境页直接给出「安装 Git」按钮（跳转国内镜像站）；缺 Node.js 给出「一键安装」引导
- 启动器自更新检查内置 GitHub 源，npm/Node 官方源失败自动回退国内镜像

## v1.4.1 (2026-08-14)

**简化 & 打磨**

- 设置页移除 4 个冗余复选框（自动检查更新 / 自动启动服务 / 自动重启旧服务 / 自动打开浏览器），全部改为内置默认行为
- 窗口默认尺寸加大（920×640），首页环境信息条完整显示
- 插件页每行显示 **本地 → 最新** 版本（git 短哈希），更新页插件列表同步展示

## v1.4.0 (2026-08-14)

**首个发布版本**

- 🎯 一键安装（Node.js + dsh，官方源失败自动回退国内镜像）
- ▶️ 一键启动（按钮随状态变身：安装/启动/打开浏览器/停止/重启）
- 🔄 三维更新策略：启动器（GitHub version.txt）/ dsh（npm）/ 插件（git），当前/最新版本直观显示，启动与每 3 小时自动后台获取
- 🧩 插件管理：git URL / npm 包名安装、单个更新、卸载、一键维护（全部更新 + 修复依赖）、可更新徽标
- 🌐 中英双语界面（默认跟随系统），导航/配置项矢量图标，语言选项带国旗
- 🖥️ 系统托盘常驻、单实例防重开、崩溃自动记录 crash.log
- 📐 高 DPI 响应式：运行时读取系统缩放系数，窗口按屏幕自动居中/收缩
- 🎬 品牌过渡动画（弹性缩放 + 光晕 + 进度条）
- 深色主题对话框（替换系统浅色弹窗）

## v1.3.x 及更早 (2026-08-13 ~ 14)

- 深色卡片式 UI、左侧导航、无边框拖拽、托盘、单实例
- 高 DPI 自适应（手动等比缩放）、停靠布局修复
- 日志查看、环境检测、设置页等基础功能
